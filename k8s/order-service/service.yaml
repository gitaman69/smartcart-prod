apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: smartcart
spec:
  selector:
    app: order-service
  type: NodePort
  ports:
    - protocol: TCP
      port: 3003
      targetPort: 3003
      nodePort: 30003